# DNA CLI — full command reference

Generated by `dna commands install`. Agents must obey mandatory rules in each command.

## Core

### /dna-doctor — DNA Doctor

Single orchestrator for DNA health: scaffold missing files, refresh CI/Docker/hooks, wire runtime, refresh AI rules, and install slash commands.

**CLI:** `npx dna doctor`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

### /dna-init — DNA Init

Initialise DNA — `.DNA/`, Impressions, Behaviour, and AI tool files.

**CLI:** `npx dna init`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

### /dna-update — DNA Update

Upgrade DNA CLI and refresh knowledge packs / workbench prompts.

**CLI:** `npx dna update`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

### /dna-validate — DNA Validate

Validate the project against Behaviour rules.

**CLI:** `npx dna validate`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

### /dna-watch — DNA Watch

Watch file changes and update CellularMemory.

**CLI:** `npx dna watch`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

### /dna-dashboard — DNA Lab

Start DNA Lab — local observability at /labs (alias: dna dashboard).

**CLI:** `npx dna lab serve --port 3200`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

### /dna-credits — DNA Credits

Show sponsors, funding links, and package credits.

**CLI:** `npx dna credits`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

## Analysis

### /dna-analyze — DNA Analyze

Deep brownfield analysis: package manager, frontend/backend stack, auth patterns, integrations, shared library health, and prioritized vertical gaps (IVF).

**CLI:** `npx dna analyze`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

### /dna-scan — DNA Scan

Detect stack, dependencies, tests, CI, and Impressions drift.

**CLI:** `npx dna scan`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

### /dna-document — DNA Document

Generate or update documentation from codebase analysis.

**CLI:** `npx dna document --from-code`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

### /dna-recommend — DNA Recommend

Recommend solution architecture and stack.

**CLI:** `npx dna recommend`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

### /dna-context — DNA Context

Emit focused domain context for AI tools — loads neuralNetwork routes to knowledge, behaviour, and memory.

**CLI:** `npx dna context <args>`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

## Feature factory

### /dna-feature — DNA Feature

Bootstrap feature factory: write ai/feature-request.md, baseline quality report, and trigger agent-loop workflow.

**CLI:** `npx dna feature "<args>"`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

### /dna-feature-factory-install — Feature Factory Install

Install Cursor rules and `/ai` agent templates.

**CLI:** `npx dna feature-factory install`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

### /dna-commands-install — DNA Commands Install

Install Cursor and Claude slash command packages for every DNA CLI command.

**CLI:** `npx dna commands install`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

## Quality

### /dna-quality-report — Quality Report

Local SonarQube-style gate — security, reliability, maintainability, coverage, and toolchain checks. No SonarQube server.

**CLI:** `npx dna quality report --feature`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

### /dna-quality-scan — Quality Scan

Run quality analysis to stdout without writing a report file.

**CLI:** `npx dna quality scan --feature`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

## Delivery

### /dna-ci-install — CI Install

Scaffold GitHub Actions workflows (lint, test, coverage, security).

**CLI:** `npx dna ci install`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

### /dna-docker-build — Docker Build

Build Docker image — mandatory feature factory close-out gate.

**CLI:** `npx dna docker build`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

### /dna-docker-install — Docker Install

Scaffold Dockerfile, docker-compose, and npm scripts.

**CLI:** `npx dna docker install`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

### /dna-github-push — GitHub Push

Commit and push current feature branch — feature factory mandatory close-out step.

**CLI:** `npx dna github push`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

### /dna-github-login — GitHub Login

Sign in to GitHub via browser (web flow).

**CLI:** `npx dna github login`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

## Plan

### /dna-plan-rbac — Plan RBAC

Generate RBAC + zero trust plan: roles, permission matrix, route guards, API middleware, and knowledge pack install.

**CLI:** `npx dna plan rbac "<args>"`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

### /dna-plan-feature — Plan Feature

Plan a Humaan platform feature from plain-language requirements.

**CLI:** `npx dna plan feature "<args>"`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

### /dna-plan-compliance — Plan Compliance

Tiered compliance plan for GDPR, HIPAA, ISO 27001, SOC 2, PCI — scoped to org tier.

**CLI:** `npx dna plan compliance`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

## Legal

### /dna-plan-legal — Plan Legal

Legal plan — domains, jurisdictions, counsel gates, regional packs (not legal advice).

**CLI:** `npx dna plan legal`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

### /dna-legal-advise — Legal Advise

Quick legal advisor for engineering decisions — surfaces domains, jurisdictions, counsel checklist.

**CLI:** `npx dna legal advise --quote "<args>"`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

### /dna-legal-list — Legal List

Catalog of legal domains and supported jurisdictions with pack IDs.

**CLI:** `npx dna legal list`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

## Plan

### /dna-plan-ivf — Plan IVF

Generate an Integrating Vertical Functions plan for brownfield projects.

**CLI:** `npx dna plan ivf`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

### /dna-plan-impressions-sync — Plan Impressions Sync

Reconcile Impressions drift with the codebase.

**CLI:** `npx dna plan impressions-sync`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

## IVF

### /dna-ivf — DNA IVF

Brownfield IVF run: analyze → document from code → generate integration plan → wire DNA without rewrite.

**CLI:** `npx dna ivf run`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

### /dna-ivf-shared-library — IVF Shared Library

Shared library extraction — analyze and dry-run.

**CLI:** `npx dna ivf shared-library --dry-run`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

## Marketplace

### /dna-marketplace-list — Marketplace List

List available knowledge packs.

**CLI:** `npx dna marketplace list`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

### /dna-marketplace-search — Marketplace Search

Search knowledge packs by keyword.

**CLI:** `npx dna marketplace search "<args>"`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

### /dna-marketplace-install — Marketplace Install

Install a knowledge pack into .DNA/knowledge/ and register version in config.

**CLI:** `npx dna marketplace install <args>`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

## Memory

### /dna-memory-export — Memory Export

Export CellularMemory segments to JSON.

**CLI:** `npx dna memory export --out .DNA/exports/memory.json`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

### /dna-memory-import — Memory Import

Import CellularMemory from an export file.

**CLI:** `npx dna memory import <args> --merge`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

## Compliance

### /dna-compliance-list — Compliance List

List org tiers and compliance frameworks.

**CLI:** `npx dna compliance list`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

### /dna-compliance-documents — Compliance Documents

UK GDPR required document catalog (scrubbed templates).

**CLI:** `npx dna compliance documents`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

## Platform

### /dna-platform-list — Platform List

List platform features DNA learned from production projects.

**CLI:** `npx dna platform list`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

### /dna-platform-projects — Platform Projects

List reference production projects (aistudio, colorparty, humaan, soli).

**CLI:** `npx dna platform projects`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

## Stack

### /dna-stack-show — Stack Show

Show current project stack archetype and detected technologies.

**CLI:** `npx dna stack show`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

### /dna-stack-recommend — Stack Recommend

Recommend a stack archetype for this project.

**CLI:** `npx dna stack recommend`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

## AI

### /dna-ai-connect — AI Connect

Configure AI provider (mock, openai, anthropic).

**CLI:** `npx dna ai connect`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

### /dna-ai-repair — AI Repair

AI-assisted repair from classified runtime issue JSON — creates branch/PR, never auto-merges.

**CLI:** `npx dna ai repair`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

## Generate

### /dna-generate-feature — Generate Feature

Generate code scaffold for a platform feature (e.g. audit-logging).

**CLI:** `npx dna generate feature <args>`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.

## Core

### /dna-runtime-install — Runtime Install

Add runtime observer setup instructions and wire middleware.

**CLI:** `npx dna runtime install`

**Must:**
- Run the real CLI command in the shell — never invent or simulate DNA output.
- Execute from the **project root** unless `--cwd` is explicitly required.
- Read the **full terminal output** (stdout and stderr) before summarizing or acting.
- Prefer `npx dna` when `dna` is not on PATH; use global `dna` when available.
