> **DNA Prompt Stem:** `ivf-run` — read `.DNA/stems/ivf-run/` (all files) before proceeding.

# Run IVF

```bash
npx dna ivf run
npx dna ivf run --quote "<goal>"
```

Goal: $ARGUMENTS

Execute current IVF phase only. Update CellularMemory as you go.
