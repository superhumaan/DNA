> **DNA Prompt Stem:** `sync-impressions` — read `.DNA/stems/sync-impressions/` (all files) before proceeding.

# Sync Impressions

```bash
npx dna scan
npx dna plan impressions-sync
```

Scope: $ARGUMENTS

Update or generate Impressions from code. Open PR if --open-pr.
