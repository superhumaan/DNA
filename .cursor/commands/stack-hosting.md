> **DNA Prompt Stem:** `stack-hosting` — read `.DNA/stems/stack-hosting/` (all files) before proceeding.

# Stack & hosting

```bash
npx dna stack show
npx dna scan
```

Explain detected stack, hosting provider, database signals, and whether `dna-preview.yml` applies (Vercel/Netlify only — not guessed).
