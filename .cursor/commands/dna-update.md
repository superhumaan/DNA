> **DNA Prompt Stem:** `keep-dna-current` — read `.DNA/stems/keep-dna-current/` (all files) before proceeding.

# Keep DNA current

```bash
npx dna update
npx dna update --check-only
```

Scope: $ARGUMENTS

Report: CLI version before/after, marketplace pack updates available, workbench stem count refreshed.
