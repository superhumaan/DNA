> **DNA Prompt Stem:** `memory-export` — read `.DNA/stems/memory-export/` (all files) before proceeding.

# Memory export

```bash
npx dna memory export --out .DNA/exports/memory.json
```

Confirm export path and segment count.
