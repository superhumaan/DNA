> **DNA Prompt Stem:** `docker-build` — read `.DNA/stems/docker-build/` (all files) before proceeding.

# Docker build

```bash
npx dna docker build
```

Report image tag, build success/failure, fix blockers if failed.
