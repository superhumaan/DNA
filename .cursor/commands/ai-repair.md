> **DNA Prompt Stem:** `ai-repair` — read `.DNA/stems/ai-repair/` (all files) before proceeding.

# AI repair

```bash
npx dna ai repair --issue '<json or description>'
```

Issue: $ARGUMENTS

Human review required before merge. Never auto-merge repair PRs.
