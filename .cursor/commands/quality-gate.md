> **DNA Prompt Stem:** `quality-gate` — read `.DNA/stems/quality-gate/` (all files) before proceeding.

# Quality gate

Scope: $ARGUMENTS

```bash
npx dna quality report --feature
```

Fix blockers until PASS. Then offer docker + github push.
