> **DNA Prompt Stem:** `github-push` — read `.DNA/stems/github-push/` (all files) before proceeding.

# GitHub push

Prerequisite: quality PASS.

```bash
npx dna github push --message "feat: <summary>"
```

Message context: $ARGUMENTS
