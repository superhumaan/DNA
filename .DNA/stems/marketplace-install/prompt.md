> **DNA Prompt Stem:** `marketplace-install` — read `.DNA/stems/marketplace-install/` (all files) before proceeding.

# Marketplace install

```bash
npx dna marketplace install <packId>
```

Pack: $ARGUMENTS

Load installed files. Summarize what rules now apply.
