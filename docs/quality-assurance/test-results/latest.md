# Latest test results

Regenerated on **`pnpm run wiki:sync`** from monorepo CI.

```bash
pnpm test
pnpm run wiki:sync
```

## Report

Run `pnpm test` in the DNA monorepo root. CI executes the same on every push/PR to `main`.

See [Testing strategy](../testing-strategy.md).
